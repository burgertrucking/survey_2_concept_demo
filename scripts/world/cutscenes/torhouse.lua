return {
    book = function(cutscene)
        cutscene:text("* (A book of hers, it's a book of hymns.)")
    end,

    chairiel = function(cutscene)
        cutscene:text("* (It's Chairiel, the beloved living room chair.)")
    end,

    couch = function(cutscene)
        cutscene:text("* (It's Couch, the beloved living room couch.)")
    end,

    diningtable = function(cutscene)
        cutscene:text("* (Most of the space on this dining table goes unused.)")
    end,

    fridge = function(cutscene)
        cutscene:text("* (The fridge door is decorated with photos of you and your brother throughout the years.)")
        cutscene:text("* (Upon opening it, you note your supply of chocolate milk is running dry.)")
    end,

    lesserchairiel = function(cutscene)
        cutscene:text("* (You've always related more with Lesser Chairiel than her elder sister.)")
    end,

    microwave = function(cutscene)
        cutscene:text("* (Paper towels line the microwave's turntable to absorb spills.)")
        cutscene:text("* (They haven't been replaced in a while.)")
    end,

    phone = function(cutscene)
        cutscene:text("* (The landline dutifully protects your family's cell numbers from spammers.)")
        cutscene:text("* (Despite its best efforts, you just can't win against the calls.)")
        cutscene:text("* (Nobody can beat the calls.)")
    end,

    sink = function(cutscene)
        cutscene:text("* (The sink is clogged with white fur.)\n* (It drains awfully slowly.)")
    end,

    stove = function(cutscene)
        cutscene:text("* (Oil and food stains mar the stovetop's surface.)\n* (Even more so inside the oven.)")
        cutscene:text("* (Cleaning up was never your strong suit.)")
    end,

    trash = function(cutscene)
        cutscene:text("* (The trash can emits its usual floral aroma.)")
    end,

    tvtime = function(cutscene)
        cutscene:text("* (The television remains unplugged.)")
    end,

    watersausage = function(cutscene)
        cutscene:text("* (Your mom's collection of water sausages.)")
    end,
}
